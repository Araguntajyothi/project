package interfaceexample2;

//we can use the dummy implementation when the real implementation is not available
//interface provides u  the way to continue the work even when the external interface is not available 
//interface also provides a way to communicate b/n two projects
public interface ComplexAlgorithm {
	int complexAlgorithm(int number1, int number2);
}
